"""`python -m tui.graph_browser`: the demo, with no setup beyond the extra.

The pricing graph lives at the repository root with the other demo scripts,
which is where someone looking for a runnable example looks first; this is the
same demo, reachable without knowing its filename. `example_browser.py`'s toy
graph is still there for exercising the display in isolation.
"""

from example_pricing_browser import main

main()
