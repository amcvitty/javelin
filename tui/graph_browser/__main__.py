"""`python -m tui.graph_browser`: the demo, with no setup beyond the extra.

The toy graph lives at the repository root with the other demo scripts, which
is where someone looking for a runnable example looks first; this is the same
demo, reachable without knowing its filename.
"""

from example_browser import main

main()
